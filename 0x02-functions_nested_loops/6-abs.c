#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: integer to be computed
 *
 * Return: 0 (Always Success)
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
